/** Customer / storefront session cookie (shared by server + client; not secret). */
export const AUTH_COOKIE_NAME = "irobox_session";

/** Admin panel session cookie. */
export const ADMIN_AUTH_COOKIE_NAME = "irobox_admin_session";
