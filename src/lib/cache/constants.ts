/** PDP data cache TTL — keep in sync with `shop/[slug]/page.tsx` `revalidate`. */
export const PRODUCT_PAGE_REVALIDATE_SECONDS = 300;

/** Build prerender cap lives in `src/lib/shop/productStaticParams.ts` (`STATIC_PDP_BUILD_LIMIT`). */
